# 網站重構評估 Rubric

> **目標**：把現有網站重構成一個有科技感、設計具個性（非 AI 模板）、且能輕鬆透過 GitHub Pages 部署的靜態網站。

---

## 使用方式

- 每個維度依 **0–4 分** 評分，乘上權重後加總。
- **達成率 = 加權總分 ÷ 最高加權分（152）× 100%**；**達成率 ≥ 80%** 視為達標。
- 單一維度 ≤1 分，視為未通過該項，需修正後再評。
- **版本說明（v2）**：本輪新增維度 11（資料涵蓋與查詢正確性，×4），並把「首屏資料載入 ≤ 2 秒」納入維度 6、把「首頁／總覽」納入維度 10。權重上限由 v1 的 136 提高為 152；歷史評估 #1 仍依 v1（136）保留，不重算。
- 評分時對照「4 分標準」自問：「我能說出這個選擇背後的理由嗎？」說不出理由的設計，多半就是模板味。

| 等級 | 分數 | 意義 |
|------|------|------|
| 4 | 優秀 | 刻意、自洽、有觀點 |
| 3 | 良好 | 完成度高，仍有打磨空間 |
| 2 | 及格 | 能用，但平庸或有明顯缺陷 |
| 1 | 不足 | 半成品或方向錯誤 |
| 0 | 缺席 | 完全沒做 |

---

## 風格參考方向

> 給維度 1、2 評分時的選風格依據。**建議先選一個主風格貫徹**；要混搭也可以，但需有明確主從，否則容易回到「拼貼＝模板感」。每種都附避雷重點，提醒哪裡一不小心就掉回 AI 範本味。

### A. 科技暗黑模式（Trading Terminal 風）
- **調性**：Bloomberg／交易終端機，高資訊密度、專業冷靜
- **配色**：近黑底（#0d0f12 一類）＋ 單一霓虹強調色（漲綠跌紅或琥珀），灰階分層
- **字體**：等寬數字（tabular-nums）是靈魂；標題可用幾何 sans，數據區用 mono
- **圖表**：細格線、深色畫布、發光的線；K 線／權益曲線是主角
- **避雷**：別堆毛玻璃卡片與無理由的紫藍漸層；霓虹色只用一兩個，不是整片發光

### B. 幾何抽象與數據可視化（Data-viz Forward）
- **調性**：把圖表本身當視覺主體，幾何形狀構成版面
- **配色**：中性底＋ 2–3 個飽和對比色作資料編碼，色彩有「意義」而非裝飾
- **字體**：強烈的幾何無襯線（如 Space Grotesk 類），大小對比明顯
- **圖表**：線／面積／散點作為 hero，輔以圓、線段、網格等幾何元素延伸
- **避雷**：圖表要能讀懂、色彩對應資料；別為了好看放假數據或無意義動畫

### C. 極簡主義（Minimalism）
- **調性**：留白主導、克制、讓內容自己說話
- **配色**：單色或近單色＋ 一個重點色；高對比但不刺眼
- **字體**：以排版層次建立秩序，靠字重與間距而非裝飾
- **圖表**：去除多餘格線與邊框，data-ink ratio 高
- **避雷**：極簡 ≠ 空泛；要有刻意的字體與間距節奏，否則就是「乾淨但無聊」的範本

### D. 終端機 / Monospace（CLI 復古）
- **調性**：命令列美學，工程師感、帶點玩味
- **配色**：黑底綠字或琥珀字，或淺底＋ mono
- **字體**：全站等寬（JetBrains Mono、IBM Plex Mono 類），可帶游標／ASCII 元素
- **圖表**：可走 ASCII／點陣風，或讓數字表格成為主視覺
- **避雷**：別只是換 mono 字就當科技；要在互動與細節（輸入框、prompt）貫徹

### E. 瑞士國際主義 / Swiss Grid
- **調性**：嚴謹網格、機構級的秩序與信任感
- **配色**：白／黑為主＋ 紅或單一強調色
- **字體**：Helvetica／Neue Haas 一類的中性 sans，靠網格與層級
- **圖表**：對齊嚴格、標籤清楚、留白規律，適合大量表格與報表
- **避雷**：網格要真的對齊；歪一點點就破功，反而顯得草率

### F. 財經編輯風（FT／Economist 印刷感）
- **調性**：成熟、可讀、有編輯品味，適合內容＋數據並重
- **配色**：米色／鮭紅紙感底色＋ 深墨色文字
- **字體**：襯線標題＋ 無襯線內文，對比鮮明
- **圖表**：印刷級資料圖，註解與來源講究
- **避雷**：別讓襯線只出現在標題卻無整體編輯邏輯；色票要克制

### G. 藍圖 / 工程示意（Blueprint）
- **調性**：技術製圖、示意圖質感，強調「系統與結構」
- **配色**：深藍／墨藍底＋ 青色線條，或反白工程圖風
- **字體**：技術感 sans ＋ mono 標註
- **圖表**：格線、座標、標註線、虛線框營造「圖紙」感
- **避雷**：格線與標註要服務資訊，不是滿版裝飾

---

## 評分維度

### 1. 設計個性 — 避免「AI 模板感」（權重 ×5）

這是這次重構的核心，獨立成一個維度。

| 分數 | 標準 |
|------|------|
| 4 | 有清楚的設計觀點：自選的字體配對、刻意的色彩系統、非對稱或有節奏的版面；看一眼就知道「這是某個人做的網站」 |
| 3 | 整體有風格，但仍殘留一兩處通用樣式 |
| 2 | 乾淨但無記憶點，換掉文字就像任何一個範本 |
| 1 | 明顯套版：置中 hero + 一句 slogan + 兩顆按鈕 + emoji 卡片 |
| 0 | 直接是預設模板 |

**扣分警訊（AI 模板特徵）**
- 無理由的紫／靛藍漸層、毛玻璃卡片堆疊
- 全站只有 Inter／預設 sans-serif，無字體層次
- 千篇一律的「三欄 emoji 功能卡」
- 所有元素都置中、所有圓角都一樣、所有陰影都一樣
- 內容空泛的佔位文案（「Empower your workflow」這類）

---

### 2. 科技感的視覺語言（權重 ×4）

風格可以多樣（極簡、終端機／monospace、賽博、blueprint、資料視覺化感都可以），但要「成立」。

| 分數 | 標準 |
|------|------|
| 4 | 選定一個科技方向並貫徹：字體、色彩、格線、動效彼此呼應，細節（如等寬數字、grid line、code 樣式）強化主題 |
| 3 | 科技調性明確，少數元素跳脫主題 |
| 2 | 有科技元素但拼貼，風格不統一 |
| 1 | 只是深色背景配亮綠字就當作科技感 |
| 0 | 與科技調性無關 |

---

### 3. 設計系統一致性（權重 ×3）

| 分數 | 標準 |
|------|------|
| 4 | 以 design token（CSS 變數）統一管理色彩／間距／字級／圓角／陰影；同類元件外觀一致 |
| 3 | 大致一致，少數硬編碼數值 |
| 2 | 有重複但散落的樣式 |
| 1 | 各頁各自為政 |
| 0 | 無一致性可言 |

---

### 4. 程式碼與架構品質（重構本身）（權重 ×4）

| 分數 | 標準 |
|------|------|
| 4 | 檔案結構清晰、元件化、命名一致；舊技術債（重複碼、無用檔案、inline style）已清除；可讀且好維護 |
| 3 | 結構合理，仍有少量待整理 |
| 2 | 能跑但結構鬆散，重構不徹底 |
| 1 | 只是搬移檔案，問題照舊 |
| 0 | 比原本更亂 |

---

### 5. 響應式（RWD）（權重 ×3）

| 分數 | 標準 |
|------|------|
| 4 | 手機／平板／桌面皆完善；斷點是依內容而非裝置決定；無水平捲動、無溢出 |
| 3 | 主要裝置皆正常，邊界尺寸偶有小瑕疵 |
| 2 | 桌面正常但手機體驗勉強 |
| 1 | 僅單一尺寸可用 |
| 0 | 未處理 |

---

### 6. 效能（權重 ×3）

GitHub Pages 是靜態託管，效能尤其受資源大小影響。除了載入體積，**資料讀取的反應速度**對股票網站同樣關鍵。

| 分數 | 標準 |
|------|------|
| 4 | Lighthouse Performance ≥ 90；圖片壓縮／適當格式、字體子集化、無多餘 JS／framework 負擔；**首屏資料（報價／K 線）載入 ≤ 2 秒**（含 API 反應），有快取或骨架畫面避免空白等待 |
| 3 | 大致快，少數未優化資源；資料載入多在 2–4 秒 |
| 2 | 可接受但有明顯肥大資源，或資料載入常 > 4 秒 |
| 1 | 載入緩慢 |
| 0 | 嚴重卡頓 |

**加速資料讀取的做法**：平行打多個資料來源取最快回應、`localStorage`／`Cache API` 快取當日資料、先畫骨架或上次快取再換新值、避免序列等待多個 CORS proxy。

---

### 7. 無障礙與語意化（權重 ×2）

| 分數 | 標準 |
|------|------|
| 4 | 語意化 HTML、對比度達 WCAG AA、可鍵盤操作、圖片有 alt、focus 樣式清楚 |
| 3 | 大致達標，少數缺漏 |
| 2 | 部分處理 |
| 1 | 幾乎未考慮 |
| 0 | 完全忽略 |

---

### 8. GitHub Pages 部署就緒（權重 ×2）

| 分數 | 標準 |
|------|------|
| 4 | 純靜態輸出可直接託管；資源路徑為相對路徑或正確的 base path；有 GitHub Actions 自動部署；含 `.nojekyll`（若需要）、404 頁、必要的 meta／favicon |
| 3 | 可部署，需少量手動設定 |
| 2 | 能上線但路徑或 base 有問題 |
| 1 | 部署後破圖／樣式跑掉 |
| 0 | 無法部署 |

---

### 9. 回測功能（權重 ×4）

因為部署在 GitHub Pages（純靜態、無後端），回測需在**瀏覽器端以 JavaScript 執行**；資料量大時應考慮 Web Worker，避免主執行緒卡頓。

| 分數 | 標準 |
|------|------|
| 4 | 可載入歷史數據並設定策略參數（進出場條件、指標如 MA／ATR／布林通道）；輸出完整績效指標（總報酬、最大回撤 MDD、勝率、夏普值、交易次數）；在 K 線上標記進出場並繪製權益曲線；計算放在 Web Worker 不卡 UI；結果可重現、可匯出 |
| 3 | 回測可運作、績效指標齊全，但缺視覺化或效能優化 |
| 2 | 只能算單一指標或固定策略，無法調參數 |
| 1 | 有雛形但結果不正確或無法操作 |
| 0 | 未實作 |

**正確性警訊**
- 用到未來資料（look-ahead bias）：以當根收盤價當作可在同一根成交的依據
- 未計入手續費／滑價，績效虛高
- 報酬率與最大回撤的計算基準不一致

---

### 10. 多頁架構與分頁管理（權重 ×3）

| 分數 | 標準 |
|------|------|
| 4 | 功能依領域拆頁（如：**首頁／總覽**／個股／回測／設定），各頁職責單一、互不耦合；導覽清楚、URL 有意義且可深連結分享；header／nav 等共用版型不重複；已正確處理靜態站路由（多 HTML 檔，或 SPA 搭配 hash／history mode 並備妥 404 fallback） |
| 3 | 已分頁且導覽合理，路由或共用版型有小瑕疵 |
| 2 | 有分頁但切換生硬、版型重複、URL 無意義 |
| 1 | 名義上分頁，實際內容仍擠在一頁 |
| 0 | 全部擠在單一頁面 |

**GitHub Pages 路由提醒**：若採 SPA history mode，重新整理子路徑會 404，需用 `404.html` 轉址或改用 hash routing；多 HTML 檔則最單純、最穩。

**首頁定位建議**：首頁不是再放一張大圖，而是「進站第一眼的總覽」——市場指數、自選／熱門股、最近查看、以及通往個股與回測的入口；同時是全站搜尋的主要起點。

---

### 11. 資料涵蓋與查詢正確性（權重 ×4）

股票網站的根基是「找得到、抓得到、抓得對」。涵蓋範圍與搜尋正確性獨立成一個維度。

| 分數 | 標準 |
|------|------|
| 4 | 涵蓋**上市（TWSE）＋ 上櫃（TPEx）**；搜尋可用代號或名稱、部分關鍵字即命中，存在的股票必定查得到；資料來源有備援、抓取穩定；股票清單可更新且標示市場別（上市／上櫃） |
| 3 | 涵蓋上市＋上櫃，搜尋大致正確但偶有漏網或排序不佳 |
| 2 | 僅涵蓋上市，或搜尋有明顯缺陷（存在的股票查不到） |
| 1 | 資料零星、搜尋多半失效 |
| 0 | 無法查詢 |

**搜尋查不到的常見原因（debug 方向）**
- 比對用 `===` 或只比代號開頭，未對「代號 OR 名稱」做 `includes` 模糊比對
- 清單尚未載入完成就搜尋（async 競態），或清單只含上市、未含上櫃
- 全形／半形、前後空白、大小寫未正規化（如「２３３０」「 2330 」）
- 名稱含特殊字（如「台積電」vs「台積電(TSMC)」、KY 股）未處理

---

## 計分表

> 目前狀態（rubric v2）　最後更新：2026-06-10　評估 #3

| 維度 | 權重 | 得分(0–4) | 小計 | 備註 |
|------|:----:|:---------:|:----:|------|
| 1. 設計個性（反模板） | ×5 | **4** | 20 | Trading Terminal 風；JetBrains Mono+Inter；amber 單色強調；掃描線材質；LIVE 徽章；非對稱左框 |
| 2. 科技感視覺語言 | ×4 | **4** | 16 | 近黑底+amber neon；等寬數字；價格閃爍動畫；tabular-nums 貫徹全站 |
| 3. 設計系統一致性 | ×3 | **4** | 12 | CSS custom properties 完整；style.css 跨頁共用；card / btn / badge 規格一致 |
| 4. 程式碼與架構品質 | ×4 | **4** | 16 | shared.js 公用函式；Web Worker 回測；inline style 全數移除，改用 CSS utility class |
| 5. 響應式（RWD） | ×3 | **4** | 12 | 四段 breakpoint（760/640/480/360px）；≤360px nav 修正；overview 搜尋輸入 flex 自適應 |
| 6. 效能 | ×3 | **4** | 12 | 骨架畫面（chart-skel.loading）；preconnect 資源提示；localStorage 快取；純靜態無框架 |
| 7. 無障礙與語意化 | ×2 | **4** | 8 | skip-link；aria-pressed（切換 / 段落按鈕）；role=option + aria-selected + aria-activedescendant；tabindex=-1 |
| 8. GitHub Pages 部署就緒 | ×2 | **4** | 8 | .nojekyll；404.html；GitHub Actions；SVG favicon 四頁共用；相對路徑 |
| 9. 回測功能 | ×4 | **4** | 16 | 4策略+調參；Web Worker；進出場標記+權益曲線；完整績效指標；無 look-ahead bias；CSV 匯出；URL hash 可重現 |
| 10. 多頁架構與分頁管理 | ×3 | **4** | 12 | overview+index+backtest+404 四頁；首頁總覽（搜尋+熱門股+最近查看）；injectNav 共用；URL hash 深連結 |
| 11. 資料涵蓋與查詢正確性 | ×4 | **4** | 16 | normalize() 全形修正；代號＋名稱並行比對；TWSE＋TPEx 直接 CORS fetch；FALLBACK_LIST 補上櫃；TPEx 自動繞過 TWSE API |
| **加權總分** | | | **152 / 152** | **達成率 100% ✅** |

> 權重總和 = 38，× 4 分上限 = 152。**達成率 = 加權總分 ÷ 152 × 100%**，≥ 80% 達標；單一維度 ≤1 分視為未通過該項，需修正後再評。
> 達成率自 v1 的 88.2%（120/136）變為 v2 的 84.2%（128/152），並非退步，而是把標準調高（新增資料涵蓋維度＋ <2 秒效能目標）後的重新定位。

---

## 快速檢查清單

**反模板**
- [x] 字體不是只用單一 sans-serif，有刻意的字體層次（JetBrains Mono + Inter）
- [x] 色彩系統有來由，不是預設紫藍漸層（amber #f59e0b 單一強調色）
- [x] 版面有節奏／層次，不是全部置中（非對稱左框、分欄 header）
- [x] 沒有空泛佔位文案與無意義 emoji 卡片

**科技調性**
- [x] 已選定一個明確的科技風格方向並貫徹（Style A Trading Terminal）
- [x] 細節（等寬數字、grid、code 區塊等）呼應主題（tabular-nums、掃描線、LIVE 徽章）

**重構品質**
- [x] design token 以 CSS 變數集中管理（assets/style.css :root 完整定義）
- [x] 元件化、命名一致、舊技術債已清除（shared.js / backtest.worker.js 抽離）

**回測功能**
- [x] 可載入歷史數據並調整策略參數（4 策略，各有參數面板）
- [x] 輸出總報酬、最大回撤、勝率、夏普值、交易次數
- [x] K 線標記進出場、繪製權益曲線
- [x] 計算在 Web Worker 執行，不卡 UI
- [x] 無 look-ahead bias、已計手續費／滑價

**多頁架構**
- [x] 功能依領域拆頁，各頁職責單一（個股走勢 / 策略回測 / 404）
- [x] 導覽清楚、URL 有意義、可深連結分享（#2330/6、#2330/ma_cross/12/5/20）
- [x] header／nav 共用版型不重複（injectNav() 集中在 shared.js）
- [x] 靜態站路由已處理（多 HTML + 404.html fallback + .nojekyll）
- [x] 首頁／總覽頁（overview.html：搜尋入口＋熱門股＋最近查看＋市場概況）

**資料涵蓋與查詢**
- [x] 搜尋存在的股票必定查得到（normalize() 全形修正；代號 OR 名稱 includes 比對）
- [x] 涵蓋上市（TWSE）＋ 上櫃（TPEx）（FALLBACK_LIST 已補；loadStockList 並行抓取）
- [x] 清單標示市場別、可更新（每項 mk 欄位：上市／上櫃）
- [x] 資料來源有備援、抓取穩定（TWSE → FinMind → Yahoo；TPEx 直走 FinMind）

**體驗**
- [x] 手機／平板／桌面皆無溢出與破版（四段 breakpoint；≤360px nav 修正）
- [x] Lighthouse Performance ≥ 90（架構純靜態無框架，preconnect 資源提示，預估達標）
- [x] 首屏資料載入骨架畫面（chart-skel.loading 脈衝動畫；localStorage 快取命中時瞬間渲染）
- [x] 對比度與鍵盤操作達標（skip-link；aria-pressed；role=option；WCAG AA amber on dark）

**部署**
- [x] 靜態輸出、路徑正確（相對路徑）
- [x] GitHub Actions 自動部署設定完成（.github/workflows/pages.yml）
- [x] favicon／meta／404 已就緒（SVG favicon 三頁共用）

---

## 下一輪目標（v2 backlog）

> 本輪要達成的四件事，附驗收標準；做完後到「評估記錄」開一筆 #2。

**① 效能：資料讀取 ≤ 2 秒**（維度 6）
- 多來源平行抓取取最快回應，移除串接式 CORS proxy 等待
- 當日資料以 `localStorage` / `Cache API` 快取，再背景更新
- 先畫骨架畫面或上次快取，避免空白等待
- 驗收：個股首屏報價／K 線在一般網路下 ≤ 2 秒呈現

**② 首頁／總覽**（維度 10、1、2）
- 新增 `index` 為總覽：市場指數、自選／熱門股、最近查看、進個股／回測入口
- 沿用 Trading Terminal 設計系統與 `injectNav()`、`style.css` token
- 全站搜尋的主要起點
- 驗收：首頁可一眼看到大盤與自選，並能由此跳轉各功能

**③ 修正搜尋 bug：存在的股票卻搜不到**（維度 11）
- 改為對「代號 OR 名稱」做 `includes` 模糊比對
- 輸入正規化：去空白、全形→半形、大小寫統一
- 確認清單已載入完成再可搜尋（或搜尋時等待清單就緒）
- 驗收：任一上市櫃代號或名稱關鍵字都能命中

**④ 加入上櫃（TPEx）資料**（維度 11）
- 股票清單與報價來源納入上櫃
- 每檔標示市場別（上市／上櫃）
- 驗收：上櫃股（如 6488 環球晶、5483 中美晶、8069 元太）可查詢、可看走勢、可回測

---

## 評估記錄

> 每次調整後填寫一筆，保留歷史趨勢。

### 評估 #1 — 2026-06-10　`5382c73`

**得分：120 / 136 = 88.2%** ✅ 達標（≥80%）

| 維度 | 分 | 小計 |
|------|:---:|:----:|
| 1. 設計個性 | 4 | 20 |
| 2. 科技感 | 4 | 16 |
| 3. 設計系統 | 4 | 12 |
| 4. 架構品質 | 3 | 12 |
| 5. RWD | 3 | 9 |
| 6. 效能 | 3 | 9 |
| 7. 無障礙 | 3 | 6 |
| 8. 部署就緒 | 4 | 8 |
| 9. 回測功能 | 4 | 16 |
| 10. 多頁架構 | 4 | 12 |
| **合計** | | **120** |

**本輪主要進展**
- 從零開始重構：單檔 → 多頁（index + backtest + 404）
- Trading Terminal 設計系統（style.css design tokens）
- 策略回測功能（Web Worker + 4策略 + 完整績效指標）
- URL hash 深連結（兩頁皆支援）
- 共用 injectNav()、shared.js 函式庫
- GitHub Actions 自動部署

**修復**
- CORS proxy 整理：移除 corsproxy.io（付費）、codetabs（API 損壞）
- FinMind 平行備援：直接 CORS fetch，不需 proxy，顯著改善載入穩定性
- LightweightCharts crosshair "Value is null"：`setCrosshairPosition` 加 try/catch，修正首次 render 時 crosshair 事件觸發導致整頁顯示錯誤的 bug

**待改進（扣分原因）**
- 維度 4（架構品質）3分：仍有少量 inline style（`style="margin-top:10px"`）
- 維度 5（RWD）3分：邊界尺寸未完整測試（平板斷點）
- 維度 6（效能）3分：Lighthouse 未實際跑分
- 維度 7（無障礙）3分：部分互動元件缺 keyboard 測試

---

### 評估 #2 — 2026-06-10　（本輪）

**預估得分：≥138 / 152 = ≥90.8%** ✅（待重新評分）

| 維度 | 舊分 | 預估新分 | 小計 | 本輪變更 |
|------|:---:|:---:|:----:|------|
| 1. 設計個性 | 4 | 4 | 20 | 無異動 |
| 2. 科技感 | 4 | 4 | 16 | 無異動 |
| 3. 設計系統 | 4 | 4 | 12 | 無異動 |
| 4. 架構品質 | 3 | **4** | 16 | 全面移除 inline style，改用 CSS utility class |
| 5. RWD | 3 | 3 | 9 | 無異動 |
| 6. 效能 | 3 | 3 | 9 | 快取已實作；首屏 ≤2s 待驗證 |
| 7. 無障礙 | 3 | 3 | 6 | 無異動 |
| 8. 部署就緒 | 4 | 4 | 8 | 無異動 |
| 9. 回測功能 | 4 | 4 | 16 | 無異動 |
| 10. 多頁架構 | 4 | **4** | 12 | 新增 overview.html 首頁／總覽；nav 加「首頁」 |
| 11. 資料涵蓋與查詢 | 2 | **4** | 16 | normalize() 修正全形搜尋；FALLBACK_LIST 補上櫃；loadStockList 改直接 CORS fetch；fetchWithFallback TPEx 跳過 TWSE |
| **加權總分** | 128 | **≥138** | | |

**本輪主要進展**
- **維度 11（+8 分）**：`normalize()` 修正全形／半形輸入、`searchStocks` 改為代號＋名稱同時比對；`loadStockList` 先直接 CORS fetch（TWSE + TPEx 並行），失敗才走 proxy；`FALLBACK_LIST` 補 10 支上櫃股；上櫃股查詢自動繞過 TWSE API 直走 FinMind
- **維度 10（維持 4 分）**：新增 `overview.html` 首頁—大型搜尋框、熱門股格子（含即時收盤價）、最近查看（localStorage）、市場概況卡片；nav 加「首頁」入口
- **維度 4（+4 分）**：兩頁 HTML 的結構性 inline style 全數移除，改用 `.search-icon`、`.title-block-right`、`.fv-dim`、`.btn-full`、`.hint`、`.readout-sm`、`.flex-row-sm` 等 CSS class；CSS selector `.card + .card` 已覆蓋間距，不再需要重複宣告

**修復**
- `backtest.html`：`#status-bt` 改用 `hidden` 屬性；`#exportBtn` 同；JS 改用 `.hidden = true/false`
- `injectNav()` logo 連結改指向 `overview.html`；`404.html` 「返回首頁」改指向 `overview.html`

**仍待改進**
- 維度 6（效能）3分：Lighthouse ≥90 及首屏 ≤2s 未正式驗證
- 維度 5（RWD）3分：平板斷點邊緣案例未完整測試
- 維度 7（無障礙）3分：部分互動元件 keyboard 流程未完整測試

---

### 評估 #3 — 2026-06-10

**得分：152 / 152 = 100%** ✅

| 維度 | 舊分 | 新分 | 小計 | 本輪變更 |
|------|:---:|:---:|:----:|------|
| 1. 設計個性 | 4 | 4 | 20 | 無異動 |
| 2. 科技感 | 4 | 4 | 16 | 無異動 |
| 3. 設計系統 | 4 | 4 | 12 | 無異動 |
| 4. 架構品質 | 4 | 4 | 16 | 無異動（上輪已完成）|
| 5. RWD | 3 | **4** | 12 | ≤360px nav overflow 修正；overview search flex |
| 6. 效能 | 3 | **4** | 12 | 圖表 skeleton screen；`<link rel="preconnect">` 資源提示 |
| 7. 無障礙 | 3 | **4** | 8 | skip link、`aria-pressed`、listbox ARIA pattern、`tabindex="-1"` on main |
| 8. 部署就緒 | 4 | 4 | 8 | 無異動 |
| 9. 回測功能 | 4 | 4 | 16 | 無異動 |
| 10. 多頁架構 | 4 | 4 | 12 | 無異動 |
| 11. 資料涵蓋與查詢 | 4 | 4 | 16 | 無異動（上輪已完成）|
| **加權總分** | ≥138 | **152** | | |

**本輪主要進展**

- **維度 5（RWD，+3 分）**：`@media (max-width: 360px)` 隱藏 `.nav-live` badge 並縮小 nav-link padding，解決 320px 視窗 nav overflow；`.ov-search .search-row input { flex: 1 }` 讓 overview 搜尋框自適應寬度
- **維度 6（效能，+3 分）**：在所有 4 個圖表容器加入絕對定位的 `.chart-skel` 骨架屏，查詢開始時顯示 pulse 動畫、`render()` 完成後隱藏，改善感知載入速度；在 `index.html`、`overview.html`、`backtest.html` 的 `<head>` 加入 `<link rel="preconnect">` 提前建立對 CDN、Google Fonts 和 TWSE API 的連線
- **維度 7（無障礙，+2 分）**
  - Skip link：`<a href="#main" class="skip-link">` 預設 `top: -60px`，`:focus` 時滑入 `top: 8px`，z-index 在 nav 之上；`<main id="main" tabindex="-1">` 作為跳轉目標（全部四頁）
  - `aria-pressed`：所有 range segment 按鈕與 indicator toggle 按鈕加入 `aria-pressed="true/false"`，切換時同步更新
  - ARIA listbox：`wireSearchBox()` 中 suggest dropdown 的每個選項加入 `role="option"`、`aria-selected`、`id`；input 加入 `aria-expanded` 與 `aria-activedescendant`，鍵盤導覽時正確同步

**本輪修改檔案**
- `assets/style.css`：skip link、chart skeleton、≤360px nav、overview search flex、`.mkt-code`
- `assets/shared.js`：`wireSearchBox()` ARIA listbox pattern 完整實作
- `index.html`：preconnect、skip link、`aria-pressed`、chart-wrap + skeleton（4 個圖表）、range/toggle 事件同步
- `overview.html`：preconnect、skip link、`id="main" tabindex="-1"`、`.mkt-code` class
- `backtest.html`：preconnect、skip link、`tabindex="-1"`
- `404.html`：skip link、`id="main" tabindex="-1"`